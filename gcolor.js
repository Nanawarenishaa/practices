const btn=document.querySelectorAll(".btn");
console.log(btn);
const boxes=document.querySelectorAll(".box");
console.log(boxes);
const boxs=document.querySelector(".main__box");
btn.forEach ((button) =>{
    button.addEventListener("click", () =>{
         let color=button.innerHTML;
        
       })

    });